# AI-Prompt-Engineering-Chatbot
An interactive web-based chatbot built using React (frontend) and Node.js with Express (backend) that connects to a local or cloud Ollama model through the LangChain ChatOllama interface. This chatbot allows users to experiment with custom system prompts (templates) and chat dynamically.
